# Per-run onboarding output is throwaway — regenerated by build-run.sh + the onboarding agent.
# The durable, committed parts are: persona-bible.md, footprint/, rubric.md, README.md,
# build-run.sh, RESULTS.md.
run/
