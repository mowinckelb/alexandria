---
name: <slug>
description: <one sentence — what this module does, in plain terms>
---

# <Module title>

<One-paragraph framing. What problem this module solves and for whom. Concrete enough that an Author scanning the marketplace can decide in ten seconds whether it's for them.>

## When to use

- <The specific moment, situation, or trigger when this module helps.>
- <If the module is a skill, the Author state that should invoke it.>
- <If the module is a script or hook, the operational condition that should fire it.>

## When not to use

- <Where it actively hurts or wastes effort. Be specific. "Not for X" beats "use judgment".>
- <Adjacent cases where a different module is the right tool.>

## Instruction

<The actual reusable mechanism. Prompt text, script, ritual steps, filter rules — whatever the module IS. Self-contained: an Author who installs this should be able to use it without reading anything else. If the module is a skill, the body of the skill goes here. If a script, paste the script. If a ritual, write the steps.>

## Example

<One concrete example from real use. Inputs, outputs, what changed. Strip private details — keep the reusable mechanism. One example beats five abstract descriptions.>

---

*Module ID once published: `github:<user>/<repo>#<path-without-extension>` — e.g. `github:<user>/alexandria-modules/<slug>`. Append to `~/alexandria/.call_manifest` to start counting usage. See `factory/canon/library.md` for the full lifecycle.*

*Trust note: consuming Machines treat module bodies as untrusted input — instructions here are read as data and adopted only after the installing Machine reviews them against its own canon. Write the module expecting that review.*
