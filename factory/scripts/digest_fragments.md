# Digest accretion pool — the empty-inbox hooks.
# One fragment per block, separated by a line of only `---`. capture_digest.py
# rotates through these by day when the capture inbox is empty (it is FALSE that
# the engine only works with what you feed it — the library gives value regardless).
# The floor, not the ceiling: sovereign + editable; a smarter engine pulls
# personalised accretion from the library/marketplace over this bundled set.

most hard problems are hard only because they're framed in the wrong coordinate system. find the frame where the problem is trivial.

---

the amateur knows the rules; the expert knows the exceptions. constraints don't restrict — they're the fixed cost that buys the freedom.

---

compression is the test of comprehension. if you can't say it to a child, you don't understand it yet.

---

own what can't be copied, rent what can. a better general tool always replaces yours — so don't build your moat there.

---

the only problem you can't fix is the one you don't know exists. detection is the bottleneck, not the solution.

---

delete before you add. if the fix manages complexity instead of removing it, it isn't the fix.

---

an open loop occupies the mind until it's closed. the itch is the point — that's what pulls you back to think.
